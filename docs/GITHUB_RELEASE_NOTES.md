# GitHub Release Notes

Initial public project release for `mayank172900/FinTaT`.

Included:

- FinTTA core implementation.
- Open-data download/build/validation scripts.
- Sample fixture data.
- Experiment runner and committed summary metrics.
- Documentation of negative result and current project decision.

Excluded from Git:

- Large generated parquet panels under `data/raw`, `data/intermediate`, and `data/processed`.
- Model checkpoints under `outputs/**/checkpoints`.

To recreate generated artifacts, run the commands in the root `README.md`.
